version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd fronted
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: fronted/build
    files:
      - '**/*'
  cache:
    paths:
      - fronted/node_modules/**/*
